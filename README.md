# Positive Afterimage

A  project for working with positive afterimage experiments/materials.

## Structure

- `src/` - project files and scripts
- `run_exp/`- code for collecting data
- `.gitignore` - files Git should ignore

## Abstract
